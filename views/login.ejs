<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Login</title>
  <style>
    * { box-sizing: border-box; }
    body {
      font-family: 'Segoe UI', sans-serif;
      background: #fff3e0;
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100vh;
      margin: 0;
    }
    .login-container {
      background: #fff;
      padding: 2rem;
      border-radius: 12px;
      box-shadow: 0 8px 20px rgba(255, 165, 0, 0.3);
      width: 350px;
    }
    .login-container h2 {
      color: #ff6f00;
      text-align: center;
      margin-bottom: 1.5rem;
    }
    .form-group {
      margin-bottom: 1rem;
    }
    .form-group a{
      color: #e65100;
      text-decoration: underline;
    }
    .form-group label {
      display: block;
      margin-bottom: 0.5rem;
      font-weight: 500;
      color: #333;
    }
    .form-group input,
    .login-btn,
    .google-btn {
      display: block;
      width: 100%;
      border-radius: 6px;
      font-size: 15px;
    }
    .form-group input,
    .login-btn {
      padding: 12px;
    }
    .form-group input {
      border: 1px solid #ffb74d;
      margin: 0;
    }
    .login-btn {
      background: #ff6f00;
      color: #fff;
      border: none;
      cursor: pointer;
      margin-top: 0.5rem;
    }
    .login-btn:hover {
      background: #e65100;
    }
    .google-btn {
      margin-top: 1rem;
      background: #fff;
      color: #444;
      border: 1px solid #ddd;
      padding: 10px;
      display: flex;
      align-items: center;
      justify-content: center;
      transition: background 0.3s;
    }
    .google-btn img {
      height: 30px;
      margin-right: 10px;
    }
    a{
      text-decoration: none;
    }
    .google-btn:hover {
      background: #f5f5f5;
    }
  </style>
</head>
<body>
  <div class="login-container">
    <h2>Login</h2>

    <form action="/user/login" method="post">
      <div class="form-group">
      <label for="email">Email</label>
      <input type="email" id="email" placeholder="you@example.com" required name="email">
    </div>

    <div class="form-group">
      <label for="password">Password</label>
      <input type="password" id="password" placeholder="Enter password" required name="password">
    </div>
    <div class="form-group">
      <label for="password">Don't have an account?<a href="/user/signin">Signin</a></label>
      
    </div>

    <button class="login-btn" type="submit">Log In</button>
    </form>

    <a href="/auth/google">
      <button class="google-btn">
      <img src="/vecteezy_google-logo-transparent-png_42165816-removebg-preview.png"
           alt="Google Logo">
      Continue with Google
    </button>
    </a>
  </div>
</body>
</html>
